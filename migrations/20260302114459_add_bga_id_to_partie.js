/**
 * Stub — this migration was already applied directly to the database.
 * The file was lost; this stub exists solely to satisfy knex's migration
 * directory integrity check. Knex will NOT re-run it.
 */
exports.up = function (knex) {
   // no-op: already applied
   return Promise.resolve();
};

exports.down = function (knex) {
   // no-op: already applied
   return Promise.resolve();
};
